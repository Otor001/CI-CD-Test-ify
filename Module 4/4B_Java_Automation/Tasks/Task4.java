package Tasks;

public class Task4 {
    public static void main(String[] args) {
        // Storing the radius of the circle
        double radius = 12.7;

        // Calculating the area using the formula Task11 = π * r^2
        double area = Math.PI * Math.pow(radius, 2);

        // Printing the result with the unit "metres"
        System.out.println("The area of the circle is " + area + " square metres.");
    }
}
