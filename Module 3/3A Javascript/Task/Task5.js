//const compare =

/*
AND --> && all must be true
OR ---> || either must be true
NOT ---> ! toggles boolean values
*/

const userName = 'Emmanuel'
const age = 20

const logic = age === 30 || userName === 'Success'